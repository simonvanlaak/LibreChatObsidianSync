name: MCP Service CI/CD

on:
  push:
    branches: [ main ]
    paths:
      - 'McpService/**'
      - '.github/workflows/mcp-service.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'McpService/**'
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  MCP_SERVICE_IMAGE: ${{ github.repository_owner }}/obsidian-sync-mcp

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      
      - name: Install dependencies
        working-directory: ./McpService
        run: |
          pip install -r requirements.txt
          pip install pytest pytest-asyncio aiofiles
      
      - name: Run unit tests
        working-directory: ./McpService
        run: |
          pytest tests/unit/ -v || echo "No unit tests found yet"
      
      - name: Run integration tests
        working-directory: ./McpService
        run: |
          pytest tests/integration/ -v || echo "No integration tests found yet"

  build-and-push:
    needs: test
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.event_name == 'workflow_dispatch')
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Extract short SHA
        id: sha
        run: echo "SHORT_SHA=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT
      
      - name: Build and push MCP Service image
        uses: docker/build-push-action@v5
        with:
          context: ./McpService
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.MCP_SERVICE_IMAGE }}:latest
            ${{ env.REGISTRY }}/${{ env.MCP_SERVICE_IMAGE }}:${{ steps.sha.outputs.SHORT_SHA }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
